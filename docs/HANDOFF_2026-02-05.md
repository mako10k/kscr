# HANDOFF 2026-02-05

Branch: `feat/eq-show-overridable`

## Context

- Goal: make `Eq`/`Show` ordinary, overridable typeclasses while keeping `deriving (Eq, Show)` working.
- A PR was created previously from `feat/eq-show-overridable`: https://github.com/mako10k/kscr/pull/77

## Current status (local working tree)

There are **local, unpushed changes** on top of `origin/feat/eq-show-overridable`.

Changed files (8):

- src/cli_impl.rs
- src/ir.rs
- src/types.rs
- src/types/typeclass_dict_passing_common.rs
- src/types/typeclass_dict_passing_rewrite.rs
- stdlib/Prelude.ks
- tests/eq_override_instance_integration.rs
- tests/runtime_show_override_custom.rs

There is also a local marker file:

- `test_done.txt` (untracked)

Test log captured at:

- `test_output.txt`

## Repro

```bash
cargo test -- --nocapture |& tee test_output.txt
```

## What is failing

From `test_output.txt`:

- Many `cli_impl` tests panic with:
  - `Msg("MVP: class constraints support only constructor/app instance heads")`
- Some tests expect `division by zero` errors but now fail the assertion (message not present).
- A large number of `lib_test::*` tests fail (IR lowering, IR run_main, typecheck, typeclass phase3, etc.).

This suggests the local changes are an incomplete/incorrect rewrite of the typeclass constraint / dict passing pipeline and/or runtime builtins.

## Notes / Recent attempts

- Attempted to apply a patch file: `git apply /tmp/local_tys_rewrite.patch` (failed; exit 101).
- `stdlib/Prelude/Rational.ks` defines a local `(==) = __primEq` to avoid importing Prelude (cycle avoidance). Rational derives `(Eq, Show)`.

## Next steps (suggested)

1. Identify which local change introduced `MVP: class constraints ...` regressions.
   - Start at `src/types/typeclass_dict_passing_rewrite.rs` and `src/types.rs`.
2. Re-run a **single failing test** with `RUST_BACKTRACE=1` and `--test-threads=1` to reduce noise.
3. If the rewrite is meant to be experimental, consider moving it to a separate feature branch and keeping `feat/eq-show-overridable` focused.

## Safety

- Do not discard local work without saving it.
- A WIP commit is recommended before further investigation.
