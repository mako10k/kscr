[package]
name = "kscr_llvm"
version = "0.3.5"
edition = "2021"

[dependencies]
kscr_ir = { path = "../kscr_ir" }

# No external dependencies needed for MVP - we'll generate LLVM IR as text

[lib]
path = "src/lib.rs"
